import React from "react";
import "./Jumbotron.css";

const Jumbotron = props => (
    <div className="jumbotron">
        <h1>Click on your favorite Looney Tunes character to begin.
        Click on an image to earn points, but don't click anymore than once!</h1>
    </div>
);

export default Jumbotron;